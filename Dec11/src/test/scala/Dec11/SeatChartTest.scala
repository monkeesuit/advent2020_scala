package sam.Advent2020.Dec11

import org.scalatest._

class SeatChartTest extends FlatSpec with Matchers {
  "SeatChart" should "return" in {
    val test = List()
    SeatChart.handle(test) shouldBe ()
  }
}
